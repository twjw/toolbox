{
	"name": "wtbx-vite-auto-alias",
	"version": "1.0.1",
	"type": "module",
	"description": "將 tsconfig.json 的 alias 同步到 vite alias 裡",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": ["dist"],
	"scripts": {
		"build": "tsup-node index.ts --platform node --format esm --dts --clean",
		"publish-package": "pnpm run build && npm publish"
	},
	"keywords": ["typescript"],
	"license": "ISC",
	"dependencies": {
		"vite": "^5.0.10",
		"jsonc-parser": "^3.2.0"
	},
	"devDependencies": {
		"@types/node": "^20.9.0",
		"typescript": "^5.2.2",
		"tsup": "^8.0.1"
	},
	"peerDependencies": {
		"vite": "^5.x.x",
		"jsonc-parser": "^3.x.x"
	}
}
