{
	"name": "@wtbx/uno-preset-rem",
	"version": "1.0.0",
	"type": "module",
	"description": "ts 編寫環境變量",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsup-node index.ts --platform node --format esm --dts --clean --minify-identifiers",
		"publish-package": "pnpm run build && npm publish"
	},
	"keywords": ["typescript"],
	"license": "ISC",
	"dependencies": {
		"@unocss/core": "0.57.7"
	},
	"devDependencies": {
		"@types/node": "^20.9.0",
		"@unocss/core": "0.57.7",
		"typescript": "^5.2.2",
		"tsup": "^8.0.1"
	},
	"peerDependencies": {
		"@unocss/core": "0.57.7"
	}
}
