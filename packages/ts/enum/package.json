{
	"name": "wtbx-enum",
	"version": "2.0.1",
	"type": "module",
	"description": "類型安全的多定義的映射物件庫 (更: package.json 更新)",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist", "readme.md"
	],
	"scripts": {
		"build-package": "tsup index.ts --format esm --dts --clean",
		"publish-package": "pnpm run build-package && npm publish"
	},
	"keywords": [
		"typescript",
		"enum",
		"map",
		"dict"
	],
	"license": "ISC",
	"devDependencies": {
		"tsup": "^8.0.2",
		"typescript": "^5.2.2"
	}
}
