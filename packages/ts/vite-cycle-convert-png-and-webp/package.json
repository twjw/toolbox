{
	"name": "wtbx-vite-cycle-convert-png-and-webp",
	"version": "1.2.0",
	"type": "module",
	"description": "實時轉換 webp, png 檔案 (更: 新增 zip-png 功能用來將初始檔案壓縮)",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": ["dist", "readme.md"],
	"scripts": {
		"build": "tsup-node index.ts --platform node --format esm --dts --clean",
		"publish-package": "pnpm run build && npm publish"
	},
	"keywords": ["typescript"],
	"license": "ISC",
	"dependencies": {
		"vite": "^5.0.10",
		"sharp": "^0.33.1"
	},
	"devDependencies": {
		"@types/node": "^20.9.0",
		"typescript": "^5.2.2",
		"tsup": "^8.0.1"
	},
	"peerDependencies": {
		"vite": ">=5",
		"sharp": ">=0"
	}
}
