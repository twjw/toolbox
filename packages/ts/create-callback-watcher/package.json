{
	"name": "wtbx-create-callback-watcher",
	"version": "1.0.1",
	"type": "module",
	"description": "回調函數追蹤 (更: package.json 更新)",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": ["dist", "readme.md"],
	"scripts": {
		"build-package": "tsup index.ts --format esm --dts --clean",
		"publish-package": "pnpm run build-package && npm publish"
	},
	"keywords": ["typescript"],
	"license": "ISC",
	"devDependencies": {
		"tsup": "^8.0.2",
		"typescript": "^5.2.2"
	}
}
